import { CountMagiciansPipe } from './count-magicians.pipe';

describe('CountMagiciansPipe', () => {
  it('create an instance', () => {
    const pipe = new CountMagiciansPipe();
    expect(pipe).toBeTruthy();
  });
});
